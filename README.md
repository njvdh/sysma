# sysma
**sysma** (System Maintenance) is a lightweight maintenance script for Debian and Ubuntu.

## Installation
```bash
sudo cp sysma /usr/local/bin/sysma
sudo chmod +x /usr/local/bin/sysma
```

## Usage
`sudo sysma [-r] [-p]`
